<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta http-equiv="X-UA-Compatible" content="ie=edge">
<link rel="stylesheet" href="/static/style.css">
<link rel="stylesheet" href="/static/bootstrap.css">
<title>익명게시판</title>
</head>
<body>
<div class="container">
  <br>
  <br>
  <h3>글 내용</h3>
  <table class="table">
    <tbody>
      <tr>
        <th>번호 <span class="ml-3 font-weight-normal"><%= matchedBoard.bId %></span></th>
        <th>제목 <span class="ml-3 font-weight-normal"><%= matchedBoard.bTitle %></span></th>
        <th>작성자 <span class="ml-3 font-weight-normal"><%= matchedBoard.bWriter %></span></th>
      </tr>
      <tr>
        <th>내용</th>
        <td colspan="2"><%= matchedBoard.bContents %></td>
      </tr>
    </tbody>
  </table>

  <br>
  <hr>
  <br>

  <h3>댓글리스트</h3>
  <table class="table">
    <tbody>
      <% matchedComments.forEach(cItem => { %>
      <tr>
        <td><%= cItem.cComment %></td>    
        <td><%= cItem.cWriter %></td>    
      </tr>
      <% }) %>
    </tbody>
  </table>

  <br>
  <hr>
  <br>

  <h3>댓글쓰기</h3>
  <form action="/view/<%= matchedBoard.bId %>" method="POST">
    <table class="table">
      <tbody>
        <tr>
          <th>내용</th>
          <td><input type="text" name="cComment" placeholder="댓글을 입력하세요" value="댓글이지롱"></td>
        </tr>
        <tr>
          <th>작성자</th>
          <td><input type="text" name="cWriter" placeholder="댓글 작성자를 입력하세요" value="댓글이지롱"></td>
        </tr>
      </tbody>
    </table>
    <button class="btn btn-outline-success" type="submit">댓글쓰기</button>
    <a href="/" class="btn btn-outline-warning">목록</a>
  </form>
</div>
</body>
</html>